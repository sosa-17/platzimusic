/**
 * @format
 */

import {AppRegistry} from 'react-native';
import PlatziMusic from './src/App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => PlatziMusic);
